export class AttrGroupMessage {
  static createOne = {
    cmd: 'attr.group.create.one',
  };

  static findOne = {
    cmd: 'attr.group.find.one',
  };

  static deleteOne = {
    cmd: 'attr.group.delete.one',
  };

  static findAll = {
    cmd: 'attr.group.find.all',
  };
}
