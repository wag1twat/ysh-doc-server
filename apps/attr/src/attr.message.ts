export class AttrMessage {
  static createOne = {
    cmd: 'attr.create.one',
  };

  static findOne = {
    cmd: 'attr.find.one',
  };

  static deleteOne = {
    cmd: 'attr.delete.one',
  };

  static findAll = {
    cmd: 'attr.find.all',
  };
}
